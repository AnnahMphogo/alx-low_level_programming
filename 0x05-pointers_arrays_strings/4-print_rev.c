#include "main.h"

/**
 * print_rev - prints a string in reverse
 * @s: string to be printed
 *Return: void
 */
void print_rev(char *s)
{
	int c = 0;

	while (s[c] != '\0')
	{
	C++;
	}
	for (i = i - 1; i >= 0; i--)
		_putchar(s[c]);

	_putchar('\n');
}
